let rec even = \l. -- takes in a list
        if Hd(l)= Nil then 0 -- if there are no values left in the list return 0
        else S length( tl(l) ) -- then return 1 plus the return value of length with the list minus the first element
