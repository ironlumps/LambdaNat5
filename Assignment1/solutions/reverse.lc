let rec reverse= \l.
	if Hd(l)=Nil then # -- if this is the end of the list then put the end symbol
	else (reverse( tl(l) ) : Hd(l) -- if this is not the end of the list call reverse again while placing hte current head of the list at the end.
