let rec append = \l1.\l2. if l1 = # then l2 else (hd l1):(append(tl l1) (l2)) in append a:b:# c:d:#

/*if this isn't the end then add the first element of this list to the function called again
if l1 is now empty add the list l2 to the end of the current list we are makin
*/
